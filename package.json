{
  "name": "blog",
  "private": true,
  "version": "0.0.1",
  "homepage": ".",
  "scripts": {
    "precommit": "lint-staged",
    "start": "gatsby develop",
    "build": "rm -rf ./public ./.cache && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "preview": "npm run build && npm run serve",
    "deploy": "npm run build && gh-pages -d public -b master -r git@github.com:jakubjafra/jakubjafra.github.io.git"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "date-fns": "^2.16.1",
    "gatsby": "^2.24.87",
    "gatsby-plugin-mailgo": "^0.10.4",
    "gatsby-theme-blog": "2.0.2",
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "theme-ui": "0.4.0-alpha.3"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "husky": "^4.2.5",
    "jest": "^26.4.1",
    "lint-staged": "^10.1.7",
    "prettier": "^2.0.5",
    "shell-quote": "^1.7.2"
  }
}
